{
  "name": "alan-accountability",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "deploy": "node -e \"const fs=require('fs'),v=JSON.parse(fs.readFileSync('version.json'));v.version++;fs.writeFileSync('version.json',JSON.stringify(v,null,2)+'\\n');console.log('Version bumped to v'+v.version)\" && vercel --prod",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "next": "14.2.5",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
